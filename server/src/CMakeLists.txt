add_subdirectory(communication)

add_library(opengloves_server STATIC opengloves_server.cpp device_discovery.h device_discovery.cpp opengloves_device.cpp)

target_include_directories(opengloves_server PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}")

target_link_libraries(opengloves_server PRIVATE opengloves_server-includes communication_probers encoding_services communication_managers)